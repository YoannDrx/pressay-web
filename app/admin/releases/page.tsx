import { AdminHeading } from "@/components/admin-heading";
import { pressayJSON } from "@/lib/pressay-api";
type Release = { app_version: string; architecture: string; distribution_channel: string; active_devices: number; active_accounts: number; last_seen_at: string };
export default async function AdminReleasesPage() { const { data } = await pressayJSON<{ releases: Release[] }>("admin/releases"); return <><AdminHeading eyebrow="MAC / ADOPTION" title="Releases" detail="Adoption par version, architecture et canal de distribution." /><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Version</th><th>Architecture</th><th>Canal</th><th>Appareils</th><th>Comptes</th><th>Dernier contact</th></tr></thead><tbody>{data?.releases.map((release) => <tr key={`${release.app_version}-${release.architecture}-${release.distribution_channel}`}><td><strong>{release.app_version}</strong></td><td>{release.architecture}</td><td>{release.distribution_channel}</td><td>{release.active_devices}</td><td>{release.active_accounts}</td><td>{date(release.last_seen_at)}</td></tr>)}</tbody></table></div></>; }
function date(value: string) { return value ? new Intl.DateTimeFormat("fr-FR", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)) : "Inconnu"; }
